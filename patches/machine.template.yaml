machine:
  disks: ${machine_disks_section}
  nodeLabels: ${k8s_node_labels_section}
  env: ${proxy_env_section}
  files: ${cert_files_section}
  install:
${grub_use_uki_cmdline_section}
    extraKernelArgs: ${extra_kernel_args_section}
  network:
    nameservers: [${dns_servers_section}]
${extra_host_entries_section}
  time:
    servers: [${ntp_servers_section}]
  kubelet:
    extraMounts: ${kubelet_extra_mounts_section}
${kubelet_extra_args_section}
${machine_registries_section}
cluster:
  discovery:
    registries:
      kubernetes:
        disabled: false
      service:
${talos_discovery_service_section}
