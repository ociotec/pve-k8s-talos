apiVersion: apps/v1
kind: Deployment
metadata:
  name: policy-reporter-oauth2-proxy
  namespace: monitoring
  labels:
    app.kubernetes.io/name: policy-reporter-oauth2-proxy
    app.kubernetes.io/instance: policy-reporter
    app.kubernetes.io/component: oauth2-proxy
    app.kubernetes.io/part-of: policy-reporter
    app.kubernetes.io/managed-by: infrastructure
    pve-k8s-talos/section: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: policy-reporter-oauth2-proxy
      app.kubernetes.io/instance: policy-reporter
  template:
    metadata:
      labels:
        app.kubernetes.io/name: policy-reporter-oauth2-proxy
        app.kubernetes.io/instance: policy-reporter
        app.kubernetes.io/component: oauth2-proxy
        app.kubernetes.io/part-of: policy-reporter
        app.kubernetes.io/managed-by: infrastructure
        pve-k8s-talos/section: monitoring
    spec:
      priorityClassName: infra-observability
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: oauth2-proxy
          image: quay.io/oauth2-proxy/oauth2-proxy:${policy_reporter_oauth2_proxy_image_tag}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: ${policy_reporter_oauth2_proxy_cpu_request}
              memory: ${policy_reporter_oauth2_proxy_mem_request}
            limits:
              cpu: ${policy_reporter_oauth2_proxy_cpu_limit}
              memory: ${policy_reporter_oauth2_proxy_mem_limit}
          args:
            - --http-address=0.0.0.0:4180
            - --provider=keycloak-oidc
            - --oidc-issuer-url=${policy_reporter_oidc_issuer}
            - --client-id=${policy_reporter_oidc_client_id}
            - --redirect-url=${policy_reporter_oauth_redirect_uri}
            - --email-domain=*
            - --scope=openid profile email
            - --code-challenge-method=S256
            - --reverse-proxy=true
            - --set-xauthrequest=true
            - --session-cookie-minimal=true
            - --skip-provider-button=true
            - --cookie-name=${policy_reporter_oauth2_proxy_cookie_name}
            - --cookie-secure=true
            - --cookie-samesite=lax
            - --upstream=static://202
%{ for group_name in policy_reporter_oauth2_proxy_allowed_groups ~}
            - --allowed-group=${group_name}
%{ endfor ~}
            - --provider-ca-file=/run/secrets/policy-reporter-oidc-ca/ca.crt
            - --use-system-trust-store=true
          env:
            - name: OAUTH2_PROXY_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: ${policy_reporter_oauth_secret_name}
                  key: client-secret
            - name: OAUTH2_PROXY_COOKIE_SECRET
              valueFrom:
                secretKeyRef:
                  name: ${policy_reporter_oauth_secret_name}
                  key: cookie-secret
          ports:
            - name: http
              containerPort: 4180
          readinessProbe:
            httpGet:
              path: /ping
              port: http
              scheme: HTTP
            periodSeconds: 10
            timeoutSeconds: 1
            successThreshold: 1
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /ping
              port: http
              scheme: HTTP
            periodSeconds: 30
            timeoutSeconds: 1
            successThreshold: 1
            failureThreshold: 3
          volumeMounts:
            - name: policy-reporter-oidc-ca
              mountPath: /run/secrets/policy-reporter-oidc-ca
              readOnly: true
      volumes:
        - name: policy-reporter-oidc-ca
          secret:
            secretName: policy-reporter-oidc-ca
---
apiVersion: v1
kind: Service
metadata:
  name: policy-reporter-oauth2-proxy
  namespace: monitoring
  labels:
    app.kubernetes.io/name: policy-reporter-oauth2-proxy
    app.kubernetes.io/instance: policy-reporter
    app.kubernetes.io/component: oauth2-proxy
    app.kubernetes.io/part-of: policy-reporter
    app.kubernetes.io/managed-by: infrastructure
    pve-k8s-talos/section: monitoring
spec:
  selector:
    app.kubernetes.io/name: policy-reporter-oauth2-proxy
    app.kubernetes.io/instance: policy-reporter
  ports:
    - name: http
      port: 4180
      targetPort: http
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: policy-reporter-oauth2-proxy
  namespace: monitoring
  labels:
    app.kubernetes.io/name: policy-reporter-oauth2-proxy
    app.kubernetes.io/instance: policy-reporter
    app.kubernetes.io/component: oauth2-proxy
    app.kubernetes.io/part-of: policy-reporter
    app.kubernetes.io/managed-by: infrastructure
    pve-k8s-talos/section: monitoring
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-buffer-size: 128k
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - ${policy_reporter_hostname}
      secretName: ${policy_reporter_tls_secret_name}
  rules:
    - host: ${policy_reporter_hostname}
      http:
        paths:
          - path: /oauth2
            pathType: Prefix
            backend:
              service:
                name: policy-reporter-oauth2-proxy
                port:
                  number: 4180
